---
name: Checkout
'on':
  push:
    branches:
      - master
  schedule:
    - cron: "13 16 * * 1"
jobs:
  run-linters:
    name: Run linters
    runs-on: ubuntu-latest

    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4
