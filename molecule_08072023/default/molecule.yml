---
role_name_check: 1
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: instance
    image: rezizter/centos8-sudo
    #command: /usr/sbin/init
    #command: ${MOLECULE_DOCKER_COMMAND:-""}
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    tmpfs:
      - /run
      - /tmp
#    cgroupns_mode: host
    privileged: true
    pre_build_image: true
provisioner:
  name: ansible
#  playbooks:
#    converge: ${MOLECULE_PLAYBOOK:-converge.yml}
